function moveZeros(arr) {
  let temp = [];
  let zeros = 0;
  for(let i = 0 ; i < arr.length ; i++){
    
    if(arr[i] !== 0  ){
       temp.push(arr[i])
    }
    else{
      zeros++;
    }
}
 return temp.concat(Array(zeros).fill(0));
}
